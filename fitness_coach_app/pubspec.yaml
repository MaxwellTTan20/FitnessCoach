name: fitness_coach_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.0
  http: ^1.2.0
  audioplayers: ^6.0.0
  path_provider: ^2.1.4
  image: ^4.1.0
  auth0_flutter: ^1.4.1
  shared_preferences: ^2.3.2
  firebase_core: ^3.0.0
  cloud_firestore: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/avatars/
    - lib/images/profile_pictures/
